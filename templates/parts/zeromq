{% if zmq_host -%}
# ZeroMQ configuration (restart-nonce: {{ zmq_nonce }})
rpc_backend = neutron.openstack.common.rpc.impl_zmq
rpc_zmq_matchmaker = neutron.openstack.common.rpc.matchmaker_ring.MatchMakerRing
rpc_zmq_host = {{ zmq_host }}
{% endif -%}
